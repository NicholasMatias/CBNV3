import styled from 'styled-components';

export const SectionContainer = styled.section`
  min-height: 100vh;
  padding-top: 120px; /* 80px navbar height + 40px extra spacing */
  scroll-margin-top: 80px; /* This ensures the section aligns properly when scrolled to */
`; 